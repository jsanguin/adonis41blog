@layout('main')

@section('content')
  <hr>
  <a class="btn btn-secondary" href="/posts">Go back</a>
  <hr>
  <h1>{{ post.title }}</h1>
  <p>{{ post.body }}</p>

  <hr/>
  <a href="/posts/edit/{{ post.id}}" class="btn btn-secondary d-block float-left">Edit</a>

<form action="{{ '/posts/' + post.id + '?_method=DELETE'}}" method="POST">
  {{ csrfField() }}
  <button type="submit" class="btn btn-danger d-block float-right">Delete</button>
</form>
@endsection
